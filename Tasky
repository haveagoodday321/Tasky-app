<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<title>Tasky – Your Personal Assistant</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>

<style>
:root{
  --primary:#6366f1;
  --secondary:#8b5cf6;
  --accent:#22c55e;
  --gold:#f59e0b;
  --bg:#0f172a;
  --card:#ffffff;
}

body{
  font-family:system-ui;
  margin:0;
  background:linear-gradient(180deg,#1e293b,#0f172a);
  color:white;
}

.container{
  max-width:480px;
  margin:auto;
  padding:18px;
}

h1{
  background:linear-gradient(90deg,#a5b4fc,#c084fc);
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
}

.card{
  background:var(--card);
  color:black;
  padding:16px;
  border-radius:16px;
  margin-top:16px;
}

input,select{
  width:100%;
  padding:10px;
  margin-top:8px;
  border-radius:10px;
  border:1px solid #ddd;
}

button{
  width:100%;
  padding:12px;
  margin-top:10px;
  border:none;
  border-radius:12px;
  background:linear-gradient(135deg,var(--primary),var(--secondary));
  color:white;
  font-weight:600;
}

.secondary{
  background:#e5e7eb;
  color:black;
}

.badge{
  background:var(--accent);
  color:white;
  padding:4px 10px;
  border-radius:999px;
  font-size:12px;
}

.premium-badge{
  background:linear-gradient(135deg,#fbbf24,#f59e0b);
}

.locked{
  filter:blur(4px);
  opacity:.5;
}

li{
  display:flex;
  justify-content:space-between;
  margin:6px 0;
}

.done{
  text-decoration:line-through;
  opacity:.6;
}

.gold{
  color:var(--gold);
  font-weight:700;
}

.calendar{
  display:grid;
  grid-template-columns:repeat(7,1fr);
  gap:4px;
  text-align:center;
  font-size:12px;
}

.day{
  padding:6px;
  background:#e5e7eb;
  border-radius:6px;
}
</style>
</head>

<body>

<div class="container">
<h1>Tasky</h1>

<div class="card">
<span class="badge">Free</span>
<h3>Add Task</h3>

<input id="taskInput" placeholder="Task"/>
<input type="date" id="deadline"/>
<select id="priority">
<option>Low</option>
<option>Medium</option>
<option>High</option>
</select>

<button onclick="addTask()">Add</button>

<ul id="taskList"></ul>

<p>Streak: <span id="streak">0</span> 🔥</p>
</div>

<div class="card">
<h3>Mini Calendar</h3>
<div class="calendar" id="calendar"></div>
</div>

<div class="card">
<span class="badge premium-badge">Premium</span>
<h3>Premium Rewards</h3>

<div id="premiumLocked" class="locked">
<p>🎁 XP points & levels</p>
<p>🤖 Smart AI scheduling</p>
<p>🔔 Auto reminders</p>
<p>📊 Productivity insights</p>
</div>

<button onclick="unlockPremium()">Try Premium</button>
</div>
</div>

<script>
let tasks=JSON.parse(localStorage.getItem("tasks")||"[]");
let streak=localStorage.getItem("streak")||0;

document.getElementById("streak").textContent=streak;

function save(){
  localStorage.setItem("tasks",JSON.stringify(tasks));
}

function requestNotify(){
  if(Notification.permission!=="granted"){
    Notification.requestPermission();
  }
}

function notify(text){
  if(Notification.permission==="granted"){
    new Notification("Tasky Reminder",{body:text});
  }
}

function addTask(){
  const text=taskInput.value;
  if(!text)return;

  const task={
    text,
    deadline:deadline.value,
    priority:priority.value,
    done:false
  };

  tasks.push(task);
  save();
  render();
  notify("Task added: "+text);
  taskInput.value="";
}

function toggle(i){
  tasks[i].done=!tasks[i].done;

  if(tasks[i].done){
    streak++;
    localStorage.setItem("streak",streak);
    document.getElementById("streak").textContent=streak;

    if(streak%5===0){
      alert("🏆 Reward unlocked! Keep going!");
    }
  }

  save();
  render();
}

function render(){
  const ul=document.getElementById("taskList");
  ul.innerHTML="";

  tasks.forEach((t,i)=>{
    const li=document.createElement("li");

    let color=
      t.priority==="High"?"red":
      t.priority==="Medium"?"orange":"green";

    li.innerHTML=`
    <span class="${t.done?'done':''}">
    ${t.text} 
    <small style="color:${color}">
    (${t.priority})
    </small>
    </span>
    <button onclick="toggle(${i})">✓</button>
    `;

    ul.appendChild(li);
  });
}

function buildCalendar(){
  const cal=document.getElementById("calendar");
  for(let i=1;i<=30;i++){
    const d=document.createElement("div");
    d.className="day";
    d.textContent=i;
    cal.appendChild(d);
  }
}

function unlockPremium(){
  document.getElementById("premiumLocked")
  .classList.remove("locked");
  alert("✨ Premium trial unlocked!");
}

requestNotify();
render();
buildCalendar();
</script>

</body>
</html>
